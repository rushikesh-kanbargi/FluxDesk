# Local development only — do NOT set NEXT_PUBLIC_API_URL in production/Vercel.
# When unset, API calls route to /api/... on the same host (correct for Vercel).
NEXT_PUBLIC_API_URL=http://localhost:4000
